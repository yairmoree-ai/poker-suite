# Poker Suite — מסמך מסירה (State of Project)
תאריך: 2026-07-14 (המשך ישיר מצ'אט קודם שגדל מדי)

## מה זה הפרויקט
אפליקציית פוקר-בית (Live) — תיעוד ידיים, HUD/סטטיסטיקות שחקנים, ניהול טורניר,
עורך טווחי preflop, replayer ליד עם שיתוף (GIF/לינק אינטראקטיבי), ייצוא ל-
PokerTracker. קבצים עיקריים: `index.html`, `auth.js`, `state.js`, `game.js`,
`render.js`, `ui.js`, `ranges.js`, `features.js`, `styles.css`.
CHANGELOG.md (פעיל, קטן) + CHANGELOG_ARCHIVE_2026-07-14.md (היסטוריה מלאה,
195KB — **אל תעלה את זה בחזרה כקובץ הפעיל**, זה מה שגרם לקריסה).

## עבודה עיקרית פתוחה כרגע: בניית טווחי preflop אמיתיים
זה הפוקוס המרכזי של הצ'אט הזה בשלב האחרון. יש שני מקורות PDF:
1. `full-preflop-charts.pdf` (PokerCoaching, יחיד) — 100bb, 9-max. **לא נעשה
   בו שימוש בסוף** — הקריאה הראשונית נכשלה בבדיקת-אימות (216 קומבינציות
   שחולצו מול 134 שהמקור הצהיר, טעות 61%) ועברנו למקור השני.
2. **`The_Ultimate_Tournament_Preflop_Guide.pdf`** (507 טבלאות GTO) — **זה
   המקור הפעיל**. 5 עומקים: 80/50/30/20/12bb. לכל עומק: RFI (8 עמדות),
   Facing RFI (הרבה זוגות עמדות), RFI-vs-3bet (עוד הרבה זוגות).

### שיטת החילוץ (מוכחת עובדת, ניתנת לשימוש חוזר)
בגלל שקריאה ויזואלית פשוטה נכשלה (ראה למעלה), נבנה pipeline אוטומטי:
1. `pdfimages -png -f N -l N` — לחלץ כל טבלה כתמונה נפרדת (לא עמוד שלם).
2. OCR עם `tesseract` (`--psm 11 -c tessedit_char_whitelist=AKQJT98765432so`)
   — מוצא תוויות-יד בביטחון גבוה (לא את כל 169, בערך 60-100 מתוכן).
3. **כיול קואורדינטות**: מהתוויות שנמצאו, fit ליניארי (או affine מלא אם צריך
   דיוק גבוה יותר) שממיר "שורה/עמודה ברשת" → "פיקסל בתמונה", עם iterative
   outlier rejection. זה נותן את המיקום המדויק של **כל 169** התאים, גם אלה
   ש-OCR פספס.
4. דגימת **הצבע הדומיננטי (mode)** בכל תא (לא פיקסל בודד — פגיע לרעש/טקסט),
   השוואה מול צבעי-מקרא אמיתיים (נמצאים על ידי ניתוח הצבעים הדומיננטיים
   בתמונה כולה, לא מוגדרים ידנית).
5. **בדיקת-אימות**: השוואת סכום הקומבינציות שחולץ מול האחוז שהטבלה עצמה
   מצהירה עליה בכותרת. זו לא בדיקה אופציונלית — היא תפסה שגיאה אמיתית
   (row/col הפוך לידי אופסוט) ותפסה גם תופעה חשובה (ראה הבא).
6. **תאים מעורבי-תדירות (mixed-frequency cells)**: חלק מהתאים לא צבע אחיד
   אלא מפוצלים (למשל 40% אדום/60% כחול) — מייצגים תדירות חלקית אמיתית מה-
   solver. דגימת "צבע דומיננטי יחיד" יכולה לפספס לגמרי אם הדגימה נופלת על
   החלק הלא-דומיננטי. נבנה detector נפרד שבודק את **התפלגות** הצבע בכל
   התא (לא רק ה-mode), ומזהה תאים עם נוכחות משמעותית של צבע שני.
   **סף ההכללה שנקבע (על ידי המשתמש, מול בדיקה ישירה בתמונה המקורית):**
   ~20-25% נוכחות = להכליל בטווח הבינארי. דוגמאות מאושרות: J8s/KTo (קצת
   פחות מחצי) ו-JTo/A9o (בערך רבע) — כולן נכנסות ל-raise.
   **חשוב:** בגלל התאים המעורבים, האחוז הסופי בטווח הבינארי **לעולם לא
   יתאים בדיוק** לאחוז שהכותרת מצהירה (שהוא ממוצע משוקלל) — זה צפוי, לא
   סימן לשגיאה.

### מצב מדויק — 80bb RFI, 9-max (מה שכבר בוצע)
**6 מתוך 7 עמדות מוזרמות ומאומתות ב-`_RANGES[9].deep`, שדה RFI בלבד**
(3bet/call/4bet של כל עמדה לא נגעו בהם, עדיין נתונים ישנים/לא-מאומתים):
- UTG: 226 קומבינציות (17.04%)
- UTG+1: 266 (20.06%)
- LJ: 320 (24.13%)
- HJ: 374 (28.21%)
- CO: 470 (35.44%)
- BTN: 726 (54.75%)

**SB — לא הוזרם, ממתין לסבב זהיר יותר.** למה: טבלת SB היא 3-כיוונית
(Raise/Call/Fold, לא 2 כמו השאר) עם 36 תאים מעורבים (הרבה יותר מ-LJ's 4).
ניסיתי להחיל את סף ה-20-25% מכנית על כל ה-36 ויצא 166 קומבינציות (12.52%)
— לא תואם אף השערה שהייתה לי לגבי היעד (8.16% מהכותרת, או 3.16% חשוד
כטעות OCR). המשתמש לא אימת אף אחד מה-36 תאים ישירות (בניגוד ל-LJ, ששם
אימת 4 ספציפיים) — **זו קפיצה גדולה מדי בלי אימות, נעצר ביודעין**.
המלצה להמשך: לבקש מהמשתמש לבדוק ספציפית 2-3 ידיים "מפתיעות" (כמו 42s
בערך 58% אדום, K6o בערך 48% אדום) לפני שממשיכים עם כל ה-36.

**עדיין לא נגענו בכלל:** Facing RFI (הרבה זוגות עמדות), RFI-vs-3bet (עוד
הרבה), וארבעת העומקים הנוספים (50/30/20/12bb) — לגמרי לא נבדקו.

## ארכיטקטורת גדלי-שולחן (openTopic חדש מהיום, חשוב להבין)
### מבנה ה-buckets
```
// state.js/ranges.js: stackDepth buckets
deep   — 75BB+
mid    — 35-74BB
midlow — תת-חלוקה של mid (כרגע רק עותק זהה, אין נתונים ייחודיים)
short  — 20-34BB
push   — פחות מ-20BB
```
מיפוי מ-5 העומקים ב-PDF: 80→deep, 50→mid, 30→short, 20→short (גבול תחתון),
12→push.

### מודל "הזזה" לגדלי שולחן קטנים (נבנה היום, `_buildShiftedRanges` ב-ranges.js)
**לפני:** `_RANGES[8]=_RANGES[9]` — הפניה **זהה ממש**, אין הבדל אסטרטגי בין
9 ל-8 שחקנים בכלל.

**עכשיו:** בכל פעם שהשולחן מתכווץ בשחקן אחד, **מסירים רק את העמדה המוקדמת
ביותר**, וכל שאר העמדות המוקדמות/אמצעיות זזות למלא את החלל. עמדות מאוחרות
(HJ, CO) **לא זזות** — מספר היריבים שפועלים אחריהן לא משתנה משמעותית.
BTN/SB תמיד "עוגן" קבוע (BTN תמיד עם בדיוק 2 שחקנים אחריו — SB,BB — לא
משנה גודל השולחן).

```
9max: UTG, UTG+1, MP, MP+1, HJ, CO   (6 עמדות פתיחה)
8max: UTG, UTG+1, LJ,      HJ, CO   (5 — MP+MP+1 התמזגו ל-LJ)
7max: UTG, UTG+1,          HJ, CO   (4 — LJ נעלמה)

8max.UTG   = 9max.UTG+1
8max.UTG+1 = 9max.MP
8max.LJ    = 9max.MP+1
8max.HJ    = 9max.HJ      (ללא שינוי)
8max.CO    = 9max.CO      (ללא שינוי)

7max.UTG   = 8max.UTG+1  (=9max.MP)
7max.UTG+1 = 8max.LJ     (=9max.MP+1)
7max.HJ    = 8max.HJ     (ללא שינוי)
7max.CO    = 8max.CO     (ללא שינוי)
```
**חשוב:** ה**שם המוצג** לשחקן נשאר תמיד "UTG" בכל גודל שולחן — רק הנתונים
מאחורי השם זזים. השחקן לא רואה שום שינוי בממשק.

**גם תוקן:** `PBN[7]` ב-state.js היה `['BTN','SB','BB','UTG','UTG+1','MP','CO']`
(דילג על HJ לגמרי, לא עקבי עם המודל) → תוקן ל-
`['BTN','SB','BB','UTG','UTG+1','HJ','CO']`.

### נקודה פתוחה שנדונה אך לא מומשה: BB/SB call/3bet לפי-עמדת-פותח
כרגע BB/SB מחזיקים טווח call/3bet/4bet **גנרי אחד** (לא מפורק לפי איזו עמדה
ספציפית פתחה) — ולכן שאלת "האם BB-vs-UTG צריך להזיז בהתאם לגודל שולחן"
לא רלוונטית כרגע בפועל. **אם/כשנוסיף** נתונים מפורקים לפי-עמדת-פותח ל-
BB/SB, יהיה צריך להחיל את `_buildShiftedRanges` גם על מפתחות ה"מול-מי",
לא רק על עמדת-הבסיס. סומן לזיכרון, לא נבנה.

## באגים אמיתיים שנתפסו ותוקנו היום (רשימה מקוצרת — פירוט מלא ב-archive)
- Call יכול היה לעלות על הערימה בפועל של השחקן (יצר צ'יפים מדומים בקופה)
- All-in פרה-פלופ דילג ישר ל-showdown בלי לפתוח פלופ/טרן/ריבר (3 באגים
  שרשרתיים בתהליך התיקון — state לא התאפס, `enterShowdownMode` typo לא
  קיים בכלל)
- SB/BB לא קיבלו idx לסידור פעולות (BB לפעמים מוצג לפני SB ב-replayer)
- רחוב שלם עם 0 פעולות רשומות היה נבלע לגמרי כולל קלף הבורד שלו (דילוג
  פלופ→ריבר, מדלג על הטרן)
- Ante הוגדר לתצוגה בלבד — מעולם לא נוכה בפועל משום שחקן. יושם לפי מוסכמת
  "BB ante" (הנחה, לא אושרה במפורש)
- `renderTableShape` היה מתכווץ כשמקלדת נפתחת (Raise/Bet input) — guard
  היה קיים רק בנקודת-כניסה אחת, לא בפונקציה עצמה
- שגיאה עצמאית ב-z-index גרמה להודעות notify() להיות מוסתרות מאחורי ה-
  replayer — גרם ל"כלום לא קורה" מבלבל בכפתורי שיתוף
- **ייצוא PokerTracker — 3 באגים נפרדים שנתפסו ברצף אחרי המסירה הראשונית:**
  1. הודעת-הצלחה סופרת "X ידיים יוצאו" גם כשחלק (או כולן) נכשלות בשקט
     (`.filter(Boolean)` מסיר תוכן ריק בלי לעדכן את המספר המדווח) — עכשיו
     מדווח במדויק כמה הצליחו/נכשלו, ועוצר לגמרי אם 0 הצליחו
  2. מספרי המושבים (`Seat 1:`,`Seat 2:`...) הוקצו לפי seatIdx פיזי גולמי,
     לא לפי סדר-שולחן אמיתי — PokerTracker מחשב עמדה (UTG/HJ/CO וכו') לכל
     שחקן על ידי הליכה סביב השולחן ממספר-מושב ה-BTN, לא מטקסט מפורש; רק
     SB/BB מקבלים עמדה "בחינם" מ-`posts blind`. תוקן עם `_sortSeatsByPos`
     (אותה פונקציה שכבר הוכחה נכונה ל-replayer)
  3. **תוקן אחרי #2 אך לפני שהמשתמש בדק שוב:** שורות ה-`posts small/big
     blind` עדיין השתמשו ב-seats הגולמי (לא sortedSeats) — יכלו לצאת
     בסדר הפוך (BB לפני SB) גם אחרי תיקון #2. תוקן להשתמש ב-sortedSeats
     גם שם. **המשתמש שלח לבדיקה אצל חבר עם PT4/PT5 בפועל — ממתין לתוצאה.**

## פיצ'רים חדשים שנבנו היום
- **Replayer** ליד (צעד-אחר-צעד, BTN למטה, סדר כפתורים LTR, badge לפעולה
  ליד המושב, מהירות 1.8s)
- **שיתוף replayer**: כ-GIF (gif.js, worker כ-blob URL) וכלינק אינטראקטיבי
  (Firebase תחת `/users/_shared_replays_/` — **לא** `/shared_replays/`
  שנחסם ב-401 כי רק `/users/*` פתוח בחוקי האבטחה)
- **ייצוא ל-PokerTracker** (פורמט PokerStars, לא CSV — CSV לא נתמך לייבוא
  ידני ב-PT4, אומת מול פורום PokerTracker הרשמי)
- **קביעת בליינדים ידנית** — `S.customBlinds` היה קיים במודל בלי שום ממשק
  לקבוע אותו בפועל. נבנה ממשק ישיר.
- LIMP% נוסף ל-HUD (popup לחיצה-ארוכה + טאב שחקנים)

## עקרונות עבודה שהוכיחו את עצמם היום (שווה להמשיך איתם)
- **לא לנחש — לבנות בדיקת-אימות** לפני שמאמינים לתוצאה (combo-count
  cross-check תפס שגיאה אמיתית פעמיים)
- **כשמשהו "בערך עובד" אבל לא מדויק — לחפור לעומק הסיבה**, לא לקבל בשקט
  (הדוגמה הכי טובה: LJ ה-12 קומבינציות שהתגלו כתאים מעורבים)
- כש-eval-ing כמה קבצי JS יחד בבדיקות node: **לשלב הכל ל-eval אחד**,
  לא לפצל לקריאות נפרדות (בעיית scoping עם `let`/`const` שחוזרת שוב ושוב)
- לתקן בעיה **במקור** (הפונקציה עצמה) לא רק בנקודת-הכניסה שגילתה אותה,
  כי בדרך כלל יש עוד נקודות-כניסה לאותה בעיה

## מה הכי הגיוני להמשיך איתו
0. **לבדוק שהתשובה מהחבר עם PT4/PT5 הגיעה תקינה** (ייצוא נשלח לבדיקה,
   ראה סעיף הבאגים למעלה — אם עדיין יש בעיה, תתחיל משם)
1. SB ב-80bb (עם אימות זהיר יותר — לבקש בדיקה של 2-3 ידיים ספציפיות קודם)
2. Facing RFI / RFI-vs-3bet לעומק 80bb (עוד לא נגענו)
3. עומק הבא (50bb) באותה שיטה מוכחת
4. 3bet/call/4bet לכל 6 העמדות שכבר עודכנו ב-RFI
